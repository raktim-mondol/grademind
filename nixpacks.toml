# Nixpacks configuration for Railway deployment
# Since Railway Root Directory is set to /server, no need for cd commands

[phases.setup]
nixPkgs = ["nodejs-18_x", "python3"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "npm start"
